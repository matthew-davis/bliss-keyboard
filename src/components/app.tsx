// Imports
import * as React from "react";
import {MainKeyboard} from "./mainKeyboard";

// Component
export const App = (): React.ReactElement => {

  return (
    <React.Fragment>
      <MainKeyboard />
    </React.Fragment>
  );
};
